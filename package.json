{
  "name": "esy-python",
  "version": "3.7.1000",
  "description": "Python  (Cpython) packaged for esy",
  "esy": {
    "buildsInSource": true,
    "build": [
      "./configure --prefix #{self.install}",
      "make"
    ],
    "install": [
      "make install"
    ]
  }
}
